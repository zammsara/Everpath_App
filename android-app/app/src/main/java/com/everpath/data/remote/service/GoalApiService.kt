package com.everpath.data.remote.service

/**
 * Contrato encargado de definir
 * todos los endpoints relacionados
 * con metas (Goals) que serán
 * consumidos mediante Retrofit.
 *
 * En este paso únicamente se crea
 * la estructura base; los endpoints
 * se incorporarán de forma progresiva
 * en los siguientes pasos de la capa
 * remota.
 */
interface GoalApiService